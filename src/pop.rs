// each province will have a collection of pop structs that represent the different groups

struct Pop {
    culture: //enum
    religion: //enum
    social_class: //hashmap
    size: //unsigned int
    basic_needs: //hashmap
    daily_needs: //hashmap
    luxury_needs: //hashmap

}